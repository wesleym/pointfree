name: lambda_gui
description: A GUI interface to Lambda Cloud.
repository: https://github.com/lambdal/lambda-cloud-dart-gui
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  openapi:
    path: api

  cupertino_icons: ^1.0.8
  openapi_generator_annotations: ^6.1.0
  go_router: ^14.6.2
  dynamic_color: ^1.7.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.14
  flutter_lints: ^5.0.0
  openapi_generator: ^6.1.0
  test: ^1.24.0
  flutter_launcher_icons: ^0.14.2

flutter:
  uses-material-design: true
